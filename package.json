{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "lerna": "^4.0.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "lerna run test --stream",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel"
  }
}
